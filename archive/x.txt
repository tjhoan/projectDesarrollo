          <!-- <img src="<%= image.path %>" alt="image_product" class="product-image" /> -->
 

  const cartJSON = JSON.stringify(cart, (key, value) => {
    if (value && value._bsontype === 'ObjectId') {
      return value.toString()
    }
    return value
  }, 2)
  
  console.log(cartJSON)